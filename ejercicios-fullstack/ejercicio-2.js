/* Dado que el valor "numA = 8" y "numB = 2":
- Muestra por consola la suma de A + b;
- Muestra el resto de dividir A entre B.
- Muestra el resultado de elevar A a B.
*/ 

'use strict';

// Declaramos dos constantes que alamcenen un valor numerico
const numA = 8;
const numB = 2;


// Mostramos por consola el resultado de la suma, el resto de dividir A entre B, y el resultado de elevar A a la potencia de B.

console.log(numA + numB);
console.log(numA / numB);
console.log(numA ** numB)
/* Crea una variable que almacene tu nombre y otra variable que almacene tu numero favorito;
- Muestra ambos por consola.
- Cambia tu numero favorito por cualquier otro numero.
- Muestra por consola el resultado del cambio.
- Muestra por consola el tipo de las variables definidas
*/

'use strict';

// Declaramos una variable (let) y una constante (const).
const nombre = "Maria";
let numero = 13;

// Mostramos el valor de ambas variables por consola
console.log(nombre);
console.log(numero);


// Modificamos nuestro numero favorito
numero = 7;


// Mostramos por consola el numero modificado
console.log(numero);

// Mostramos por consola el tipo de variable que contienen
console.log(typeof(nombre));
console.log(typeof(numero));